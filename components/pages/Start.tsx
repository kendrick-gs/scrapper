"use client";

import Step1InputForm from '@/components/steps/step1';

// Temporary wrapper while migrating; actual Step1InputForm logic will be inlined later if needed.
export default function StartPageContent() {
  return <Step1InputForm />;
}
